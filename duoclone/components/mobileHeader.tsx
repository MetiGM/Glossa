export const MobileHeader = ( ) => {
    return (
        <nav className="lg:hidden px-6 h-[50px] flex items-center bg-amber-300 border-b fixed top-0 w-full z-50">
            Mobile Header
        </nav>
    )
} 